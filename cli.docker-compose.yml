version: "3.4"

services:
  app:
    build:
      context: .
      dockerfile: .docker/app/Dockerfile
      target: development
    stdin_open: true
    tty: true
    volumes:
      - .:/app

  solr:
    volumes:
      - $PWD/solr/conf:/opt/solr/conf
