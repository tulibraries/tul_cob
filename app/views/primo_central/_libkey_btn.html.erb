<% if with_libkey? && document["doi"].present? %>
  <%= link_to t("articles.libkey_button_html"), article_doi_path(article_doi: document["doi"].first), class: "btn btn-sm online-btn libkey-btn", target: "_blank" %>
<% end %>
