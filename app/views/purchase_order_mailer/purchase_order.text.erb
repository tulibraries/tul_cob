We have received your request for the following title. Access is generally granted within 2 business days. You will receive an email notification when the title is available for use.

Requested by <%= "#{@from[:name]}" %>
Email: <%= "#{@from[:email]}" %>

<%= @document.to_email_text %>
<%= t('blacklight.email.text.url', :url =>polymorphic_url(@document, @url_gen_params)) %>
<%= t('blacklight.email.text.message', :message => @message) %>
