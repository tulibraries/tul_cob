<div class="row no-gutters">

  <section id="content" class="col <%= "col-md-8 col-lg-9 bg-white order-last " if show_sidebar? %>ms-sm-0 ps-md-4 pe-md-3 pe-lg-5 search-results-display">
    <% unless has_search_parameters? %>
      <%# if there are no input/search related params, display the "home" partial -%>
      <%= render "home" %>
      <%= render "shared/sitelinks_search_box" %>
    <% else %>
      <%= render "search_results" %>
      <%= render :partial => "lib_guide_section" %>
    <% end %>
  </section>

  <% if show_sidebar? %>
    <section id="sidebar" class="col-md-4 col-lg-3 ps-lg-4 order-first" role="complementary">
      <% if controller_name == "databases" %>
        <div class="mb-3 text-center">
          <%= link_to(t("databases.link_text"), t("databases.link_href"), target: "_blank", class: "database-browse-link") %>
        </div>
      <% end %>
      <div class="rounded bg-white">
        <%= render "search_sidebar" %>
      </div>
    </section>
  <% end %>

</div>
