
<% doc_presenter = index_presenter(document) %>

<dl class="row document-metadata dl-invert mb-0">
<% index_fields(document).each do |field_name, field| -%>
  <% if should_render_index_field? document, field %>
    <% unless field[:no_label] %>
    <dt class="index-label col-md-3 blacklight-<%= field_name.parameterize %>"><%= render_index_field_label document, field: field_name %></dt>
    <dd class="col-md-5 blacklight-<%= field_name.parameterize %>">
    <% end %>
      <% if field[:raw] %>
        <%= raw doc_presenter.field_value(field) %>
      <% else %>
        <%= doc_presenter.field_value(field) %>
      <% end -%>
    </dd>
  <% end -%>
<% end -%>
</dl>
<dl class="row blacklight-availability availability-ajax-load" data-availability-ids="<%= document.alma_availability_mms_ids.join(',') %>" ></dl>


<%= render_availability(document, doc_presenter) %>
