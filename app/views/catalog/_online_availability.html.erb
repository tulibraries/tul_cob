<div class="card online-card rounded-0 border-0 mb-3">
  <div id="record-page-online" class="mb-0">
    <h3 class="online-card-heading card-title mb-0">Online</h3>
      <% online_resources.each do |value| %>
        <div class="record-page-online-list border border-bottom-0"><%= safe_join([value]) %></div>
      <% end %>
  </div>
</div>
