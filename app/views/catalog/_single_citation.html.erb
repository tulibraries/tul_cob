<div class="cite-modal p-4">
  <h3 class="modal-title citation-title"><%= document_heading(document) %></h3>
  <% document.citations.each do |format, citation| %>
    <% unless format == "NULL" %>
      <h4><%= citation_labels(format) %></h4>
    <% end %>
    <%= citation %>
  <% end %>
</div>
