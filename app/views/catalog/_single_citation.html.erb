<div>
  <h2 class="citation-heading"><%= document_heading(document) %></h2>
  <% document.citations.each do |format, citation| %>
    <% unless format == "NULL" %>
      <h3 class="citation-heading"><%= citation_labels(format) %></h3>
    <% end %>
    <%= citation %>
  <% end %>
</div>
