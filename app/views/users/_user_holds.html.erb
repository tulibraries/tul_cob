<div class="container user-holds" data-controller="holds" data-holds-url=<%= user_holds_path %>>
  <h2 class="user_section_title">My Requests</h2>
  <table class="table user-table table-responsive border-0">
    <caption class="sr-only">Table of Current Fines for this user</caption>
    <thead>
      <tr>
        <th width="2%">#</th>
        <th width="40%">Title</th>
        <th width="10%">Held Until</th>
        <th width="15%">Status</th>
        <th width="28%"></th>
      </tr>
    </thead>
    <% if browser.ie?("<11") %>
    <tbody>
      <%= render partial: "holds_details", locals: {holds: current_user.holds} %>
    </tbody>
    <% else %>
    <tbody data-target="holds.table">
      <p class="text-center" data-target="holds.spinner">
        <span class="loading-message glyphicon glyphicon-refresh spinning" >
        </span><span class="loading-message"> Loading Holds...</span>
      </p>
    </tbody>
    <% end %>
  </table>
</div>
