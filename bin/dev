#!/usr/bin/env bash

export RAILS_ENV="${RAILS_ENV:-development}"

if [ -f ./.env.local ]; then
	ENV_FILE=".env.local"
else
	ENV_FILE=".env"
fi

exec bundle exec dotenv -f $ENV_FILE -- bundle exec foreman start -e /dev/null -f Procfile.dev "$@"
